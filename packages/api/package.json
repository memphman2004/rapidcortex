{
  "name": "@rapid-cortex/api",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "deploy:surge:dev": "sam deploy --template-file ../../infra/cloudformation/surge-stack.yaml --stack-name rapid-cortex-surge-dev --no-confirm-changeset",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.964.0",
    "@aws-sdk/client-sns": "^3.964.0",
    "@aws-sdk/lib-dynamodb": "^3.964.0",
    "nanoid": "^5.0.9",
    "rapid-cortex-security": "1.0.0",
    "rapid-cortex-shared": "1.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.157",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
