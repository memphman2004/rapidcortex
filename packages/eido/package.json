{
  "name": "@rc/eido",
  "version": "1.0.0",
  "private": true,
  "description": "NENA-STA-021 Emergency Incident Data Object types and parse/build helpers. No runtime dependencies.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npx vitest run --root ../.. packages/eido"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
