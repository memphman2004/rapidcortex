# Monthly observation cover sheet — YYYY-MM

- Operator (role):
- `soc2-observation-pack.sh` path:
- CloudTrail `IsLogging` / log-file validation:
- Cognito `MfaConfiguration`:
- PITR sample (tables checked):
- WAF logging:
- ACM alarm state:
- Failures / AccessDenied:
- Tickets opened:
- Control-check row updated? (yes/no):
