/**
 * Alias for incident media persistence — use when adopting the launch naming (`mediaRepository`).
 * Implementation remains {@link ./incidentMediaRepository.ts}.
 */
export { IncidentMediaRepository, IncidentMediaRepository as MediaRepository } from "./incidentMediaRepository.js";
