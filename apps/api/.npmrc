# Prevent SAM from reaching the network during build
prefer-offline=true
audit=false
fund=false
loglevel=error
# Avoid monorepo workspace placeDep loops (vendor-packs vs packages/*)
workspaces=false
install-links=false
