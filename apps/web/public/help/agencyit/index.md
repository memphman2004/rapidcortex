# Agency IT overview

You handle integrations, API keys, MFA, and login issues. Agency Admin owns users and billing. You should not live in the dispatcher workspace.

If a user lands on the wrong vertical (campus vs 911), check `custom:role` first.
