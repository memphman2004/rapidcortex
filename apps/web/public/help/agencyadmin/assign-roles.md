# Assigning and changing roles

1. Open the user in **Users** or **Roles**.
2. Change `custom:role` to the canonical Rapid Cortex role. Never use `commsupervisor` (deprecated).
3. Save. The user may need to sign out and back in.

Promoting a dispatcher to supervisor removes dispatcher-only tools and adds floor tools. Test on a spare account first if you can.
