#!/usr/bin/env bash
set -euo pipefail

ENVIRONMENT="${1:-dev}"
HOST="${2:-localhost}"
PORT="${3:-2575}"

echo "test-hl7-listener: not implemented yet."
echo "Would send a test HL7 ADT message to ${HOST}:${PORT} (${ENVIRONMENT})."
exit 1
