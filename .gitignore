# See https://git-scm.com/docs/gitignore

# dependencies
/node_modules
# SAM CLI build artifact cache (sam build --build-dir default)
.rapid-cortex-sam-build/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# packaged customer documentation (npm run package:customer-docs)
/dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# MapKit JS credentials (Electron desktop)
packages/desktop/keys/*.p8
packages/desktop/.env.mapkit

# env files (can opt-in for committing if needed)
.env*
!.env.example
!.env.deploy.dev.example

# Local SSR deploy helpers (contain/surface account identifiers; source only)
scripts/env-web-ssr-*.sh
!scripts/env-web-ssr-*.example.sh
scripts/.deploy-secrets.local.sh
# Local API deploy env (account ARNs / stack output fetch; source only)
scripts/env-api-dev.sh
scripts/env-api-staging.sh
!scripts/env-api-*.example.sh
# Local macOS Developer ID + notarytool credentials (never commit)
scripts/env-mac-signing.sh

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# dependencies
node_modules/
apps/api/node_modules/

# Next.js / build output
.next/
apps/web/.next/
apps/web/out/
out/
dist/
build/

# AWS SAM / temp
.aws-sam/
infra/.aws-sam/
.sam-build-dir/
.tmp-make-artifacts/

# environment / secrets
.env
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*.p12
rapidcortex-prod-*.json

# editor/system
.DS_Store
.vscode/
.cursor/

# archives/artifacts
*.zip
*.tgz
!apps/api/vendor-packs/*.tgz
artifacts/


# internal marketing assets (large videos — keep local, not in git)
Rapid Cortex Internal Docs/Sales_Marketing/Videos/

# additional local/sensitive files
*.env
*.local
*.log

# macOS app builds
*.app/

# security scanner cache/config if local only
.semgrep/

# random/temp
ziKUSoeO


# Office temp files
~$*

# compiled/published desktop output
apps/desktop-windows/src/RapidCortexDesktop.Wpf/publish/

# production/staging local config files
apps/desktop-windows/src/RapidCortexDesktop.Wpf/appsettings.Production.json
apps/desktop-windows/src/RapidCortexDesktop.Wpf/appsettings.Staging.json
apps/desktop-windows/src/RapidCortexDesktop.Wpf/appsettings.Development.json
apps/desktop-windows/src/RapidCortexDesktop.Wpf/appsettings.json

# 10DLC filings carry the company EIN and registration ids; only the templates are tracked
scripts/aws-10dlc/*.json
!scripts/aws-10dlc/*.example.json
.aws-10dlc-state.json

# PSAP address enrichment resume file
.psap-enrich.checkpoint

# Stress test output
results/*.json
results/*.log
results/stress-report.html
!results/.gitkeep

