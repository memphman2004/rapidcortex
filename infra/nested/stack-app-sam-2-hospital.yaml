AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Rapid Cortex — hospital routing, portal, emergency connect (routes attach to stack 2 HttpApi via ApiGatewayV2)

Parameters:
  DeploymentStage:
    Type: String
    AllowedValues: [dev, staging, prod, pilot]
  HttpApiId:
    Type: String
  ImportedCognitoUserPoolId:
    Type: String
  ImportedCognitoWebClientId:
    Type: String
  ImportedCognitoIssuer:
    Type: String
  AuditTable:
    Type: String
  AgenciesTable:
    Type: String
  IncidentTimelineTable:
    Type: String
  # Existing dev tables (owned by rapid-cortex-2-dev); reference by name only.
  HospitalPreAlertsTable:
    Type: String
    Default: rapid-cortex-hospital-prealerts-dev
  HospitalCapacityTable:
    Type: String
    Default: rapid-cortex-hospital-capacity-dev
  HospitalProfilesTable:
    Type: String
    Default: rapid-cortex-hospital-profiles-dev
  ManagedPolicyNamePrefix:
    Type: String
    Default: rapid-cortex

Globals:
  Function:
    Runtime: nodejs20.x
    Timeout: 30
    MemorySize: 512
    Environment:
      Variables:
        DEPLOYMENT_STAGE: !Ref DeploymentStage
        AUDIT_TABLE: !Ref AuditTable
        AGENCIES_TABLE: !Ref AgenciesTable
        COGNITO_USER_POOL_ID: !Ref ImportedCognitoUserPoolId
        COGNITO_CLIENT_ID: !Ref ImportedCognitoWebClientId
        COGNITO_REGION: !Ref AWS::Region
        ALLOW_UNAUTHENTICATED_API: "false"

Resources:
  AppManagedPolicyHospitalDynamo:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      ManagedPolicyName: !Sub "${ManagedPolicyNamePrefix}-sam2-hospital-ddb-${DeploymentStage}"
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: HospitalAndSharedTables
            Effect: Allow
            Action:
              - dynamodb:GetItem
              - dynamodb:BatchGetItem
              - dynamodb:BatchWriteItem
              - dynamodb:PutItem
              - dynamodb:DeleteItem
              - dynamodb:UpdateItem
              - dynamodb:Query
              - dynamodb:Scan
              - dynamodb:DescribeTable
              - dynamodb:ConditionCheckItem
            Resource:
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${HospitalPreAlertsTable}
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${HospitalPreAlertsTable}/index/*
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${HospitalProfilesTable}
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${HospitalProfilesTable}/index/*
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${HospitalCapacityTable}
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${HospitalCapacityTable}/index/*
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${IncidentTimelineTable}
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${IncidentTimelineTable}/index/*
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${AuditTable}
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${AuditTable}/index/*
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${AgenciesTable}
              - !Sub arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/${AgenciesTable}/index/*

  EmergencyConnectHttpFunction:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: makefile
      BuildTarget: build-function
    Properties:
      CodeUri: ../../apps/api/
      Handler: dist/handlers/emergencyConnectHttp.handler
      Environment:
        Variables:
          ENABLE_EMERGENCY_CONNECT: "true"
          HOSPITAL_PREALERTS_TABLE: !Ref HospitalPreAlertsTable
          HOSPITAL_PROFILES_TABLE: !Ref HospitalProfilesTable
          INCIDENT_TIMELINE_TABLE: !Ref IncidentTimelineTable
          EMERGENCY_CONNECT_MOCK: "false"
          EMERGENCY_CONNECT_SEED_DEMO: "true"
      Policies:
        - !Ref AppManagedPolicyHospitalDynamo

  HospitalRoutingHttpFunction:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: makefile
      BuildTarget: build-function
    Properties:
      CodeUri: ../../apps/api/
      Handler: dist/handlers/hospitalRoutingHttp.handler
      Environment:
        Variables:
          ENABLE_HOSPITAL_ROUTING: "true"
          HOSPITAL_CAPACITY_TABLE: !Ref HospitalCapacityTable
          HOSPITAL_PROFILES_TABLE: !Ref HospitalProfilesTable
          HOSPITAL_ROUTING_MOCK: "false"
          HOSPITAL_ROUTING_SEED_DEMO: "true"
      Policies:
        - !Ref AppManagedPolicyHospitalDynamo

  HospitalPortalHttpFunction:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: makefile
      BuildTarget: build-function
    Properties:
      CodeUri: ../../apps/api/
      Handler: dist/handlers/hospitalPortalHttp.handler
      Environment:
        Variables:
          ENABLE_HOSPITAL_ROUTING: "true"
          HOSPITAL_CAPACITY_TABLE: !Ref HospitalCapacityTable
          HOSPITAL_PROFILES_TABLE: !Ref HospitalProfilesTable
          HOSPITAL_ROUTING_MOCK: "false"
      Policies:
        - !Ref AppManagedPolicyHospitalDynamo
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:AdminCreateUser
                - cognito-idp:AdminAddUserToGroup
              Resource: !Sub "arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${ImportedCognitoUserPoolId}"

  EmergencyConnectHttpIntegrationR2:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApiId
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${EmergencyConnectHttpFunction.Arn}/invocations
      PayloadFormatVersion: "2.0"

  HospitalRoutingHttpIntegrationR2:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApiId
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${HospitalRoutingHttpFunction.Arn}/invocations
      PayloadFormatVersion: "2.0"

  HospitalPortalHttpIntegrationR2:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref HttpApiId
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${HospitalPortalHttpFunction.Arn}/invocations
      PayloadFormatVersion: "2.0"

  EmergencyConnectHttpInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref EmergencyConnectHttpFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApiId}/*

  HospitalRoutingHttpInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref HospitalRoutingHttpFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApiId}/*

  HospitalPortalHttpInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref HospitalPortalHttpFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${HttpApiId}/*

  # Recreate integrations/routes (R2): CFN still holds stale HttpApi physical IDs after
  # API Gateway drift — GetRoute/GetIntegration return NotFound. Renaming logical IDs
  # forces CREATE of live routes on stack 2 HttpApi (t4bdwpjfs5).
  RouteEcGetPrealertsR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/prealerts", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcPostPrealertsR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals/prealerts", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcGetPrealertIdR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/prealerts/{alertId}", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcPatchPrealertIdR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "PATCH /api/hospitals/prealerts/{alertId}", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcPostPrealertSendR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals/prealerts/{alertId}/send", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcPostPrealertAckR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals/prealerts/{alertId}/acknowledge", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcPostPrealertCancelR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals/prealerts/{alertId}/cancel", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcGetHospitalsR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcPostHospitalsR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcGetHospitalIdR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/{hospitalId}", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }
  RouteEcPatchHospitalIdR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "PATCH /api/hospitals/{hospitalId}", Target: !Sub "integrations/${EmergencyConnectHttpIntegrationR2}" }

  # Hospital routing routes
  RouteHrGetCapacityR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/capacity", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrGetHospitalCapacityR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/{hospitalId}/capacity", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrPutHospitalCapacityR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "PUT /api/hospitals/{hospitalId}/capacity", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrPostRecommendationsR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals/recommendations", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrPostMciDistributionR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals/mci/distribution", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrGetMciIncidentR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/mci/{incidentId}", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrPostMciActivateR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospitals/mci/{incidentId}/activate", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrGetMciStatusR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/mci/{incidentId}/status", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrGetAnalyticsR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/{hospitalId}/analytics", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrGetAnalyticsPerfR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/{hospitalId}/analytics/performance", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }
  RouteHrGetLeaderboardR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospitals/analytics/leaderboard", Target: !Sub "integrations/${HospitalRoutingHttpIntegrationR2}" }

  # Hospital portal routes
  RouteHpGetContextR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospital-portal/context", Target: !Sub "integrations/${HospitalPortalHttpIntegrationR2}" }
  RouteHpPostCapacityR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospital-portal/capacity", Target: !Sub "integrations/${HospitalPortalHttpIntegrationR2}" }
  RouteHpGetCapacityHistoryR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "GET /api/hospital-portal/capacity/history", Target: !Sub "integrations/${HospitalPortalHttpIntegrationR2}" }
  RouteHpPostUsersR2:
    Type: AWS::ApiGatewayV2::Route
    Properties: { ApiId: !Ref HttpApiId, RouteKey: "POST /api/hospital-portal/users", Target: !Sub "integrations/${HospitalPortalHttpIntegrationR2}" }

Outputs:
  HospitalCapacityTableName:
    Value: !Ref HospitalCapacityTable
  HospitalProfilesTableName:
    Value: !Ref HospitalProfilesTable
  HospitalPreAlertsTableName:
    Value: !Ref HospitalPreAlertsTable
